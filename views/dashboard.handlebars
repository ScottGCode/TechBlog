{{#if logged_in}}
<section class="user-dashboard">
    <h2>Your Dashboard {{User}} !</h2>

    {{#if posts.length}}
    {{#each posts}}
    <article class="dashboard-post">
        <h3>{{title}}</h3>
         <p class="post-meta">Posted on {{#if createdAt}}{{format_date createdAt}}{{else}}Invalid Date{{/if}}</p>
        <p>{{{content}}}</p>
        <div class="dashboard-buttons">
            <a href="/api/posts/{{id}}">Edit</a>
            <button class="delete-post" data-post-id="{{id}}">Delete</button>
        </div>
    </article>
    {{/each}}
    {{else}}
    <p>You haven't created any posts yet.</p>
    {{/if}}

    <a href="/newpost">Create a New Post</a>
</section>
{{else}}
    <p>You must be logged in to view your dashboard.</p>
{{/if}}